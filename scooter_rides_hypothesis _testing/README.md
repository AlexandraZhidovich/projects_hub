# Статистический анализ данных сервиса аренды самокатов

## Описание проекта
Популярный сервис аренды самокатов нацелен на рост бизнеса.
Пользователи сервиса совершают поездки, арендуя самокаты через приложение.
Есть два тарифа: с подпиской (ultra) и без подписки (free).

Данные представлены сервисом аренды самокатов: 
выборки пользователей из нескольких городов и информация об их поездках.

Задача: 
проанализировать гипотезы, направленные на привлечение большего числа пользователей с подпиской.

## Описание данных

**Пользователи**
| Поле | Описание |
|---|---|
| `user_id` | Уникальный идентификатор пользователя |
| `name` | Имя пользователя |
| `age` | Возраст |
| `city` | Город |
| `subscription_type` | Тип подписки (`free`, `ultra`) |

**Поездки**
| Поле | Описание |
|---|---|
| `user_id` | Уникальный идентификатор пользователя |
| `distance` | Расстояние, которое пользователь проехал в текущей сессии (в метрах) |
| `duration` | Продолжительность сессии (в минутах) — время с момента нажатия кнопки «Начать поездку»

**Подписки**
| Поле | Описание |
|---|---|
| `subscription_type` | Тип подписки |
| `minute_price` | Стоимость одной минуты поездки по данной подписке |
| `start_ride_price` | Стоимость начала поездки |
| `subscription_fee` | Стоимость ежемесячного платежа |

## Этапы анализа данных
👁️ **Первый взгляд на данные**  
Первичная оценка структуры данных, обзор предоставленной информации. 

⚙️ **Предобработка данных**  
Выявление дубликатов, создание новых столбцов для расширенного анализа данных.  

🔍 **Исследовательский анализ данных (EDA)**  
География использования сервиса, распределение пользователей по типу подписки, 
демографический анализ, оценка средней дистанции и средней продолжительности поездки.  

📊 **Статистический анализ**  
- в разрезе типа подписки сравнение метрик поездок (расстояние, время), расчёт общей выручки;  
- проверка ключевых гипотез о взаимосвязи параметров.  

💡 **Вывод**  
Обобщение результатов для принятия бизнес-решений.  

🚀 **Расчёт вероятности успеха маркетинговых кампаний**  
- оценка эффективности акции с раздачей промокодов (прогноз конверсии);  
- анализ потенциальной отдачи от рассылки уведомлений (прогноз вовлечённости пользователей).  

## 📚 Используемые библиотеки Python

- **pandas** (`import pandas as pd`)
- **matplotlib** (`import matplotlib.pyplot as plt`)
- **seaborn** (`import seaborn as sns`)
- **numpy** (`import numpy as np`)
- **scipy** (`from scipy import stats as st`)
- **scipy.stats** (`from scipy.stats import f_oneway, levene`)
